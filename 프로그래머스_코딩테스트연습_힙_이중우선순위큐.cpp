#include <iostream>
#include <list>
#include <vector>

using namespace std;

vector<int> solution(vector<string> operations) {
    vector<int> answer;
    string = op;
    for(int i = 0; i < operations.size(); i++){
        if(operations[i][0] == 'I'){
            
        }
    }







    return answer;
}